import { Box, Typography, useTheme, useMediaQuery } from "@mui/material";

const ProfilePage = () => {
    return (
        <Typography>
            Hello there!
        </Typography>
    );
};

export default ProfilePage;
