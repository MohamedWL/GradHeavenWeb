import { Box, Typography, useTheme, useMediaQuery } from "@mui/material";

const ProfilePage = () => {
    return (
        <Typography>
            Good morning
        </Typography>
    );
};

export default ProfilePage;
