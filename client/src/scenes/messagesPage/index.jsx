import { Box, Typography, useTheme, useMediaQuery } from "@mui/material";

const MessagesPage = () => {
    return (
        <Typography>
            Hello there!
        </Typography>
    );
};

export default MessagesPage;
