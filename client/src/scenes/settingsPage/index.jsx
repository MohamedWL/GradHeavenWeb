import { Box, Typography, useTheme, useMediaQuery } from "@mui/material";

const SettingsPage = () => {
    return (
        <Typography>
            Hello there!
        </Typography>
    );
};

export default SettingsPage;
