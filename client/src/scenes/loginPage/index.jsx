import { Box, Typography, useTheme, useMediaQuery } from "@mui/material";

const LoginPage = () => {
    return (
        <Typography>
            Hello there!
        </Typography>
    );
};

export default LoginPage;